import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  // Default palette colors changes
  palette: {
    primary: {
      main: '#3f51b5',
    },
    secondary: {
      main: '#d80000',
    },
    otherColor:{
        main:"#999" 
    }

  },
});
